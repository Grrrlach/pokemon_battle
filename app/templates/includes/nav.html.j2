    <h1>
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <div class="container-fluid">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">

                        {% if request.path not in pages_with_no_navbar or current_user.is_authenticated %}
                          <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{url_for('main.index')}}">PokeHome</a>
                          </li>
                          {% if not current_user.is_authenticated %}
                            <li class="nav-item">
                              <a class="nav-link active" aria-current="page" href="{{url_for('auth.login')}}">Login</a>
                            </li>
                          {% endif %}
                          <li class="nav-item">
                            <a class="nav-link" href="{{url_for('auth.register')}}">
                            {% if current_user.is_authenticated %}
                              | Edit Profile |
                            {% else %}
                              Register
                            {% endif %}
                            </a>
                          </li>                        
                          {% if current_user.is_authenticated %}
                            <li class="nav-item">
                              <a class="nav-link" href="{{url_for('main.pokemon')}}">Lookup</a>
                            </li>               
                            <li class = "nav-item">
                              <a class= "nav-link" href="{{url_for('social.show_users')}}">| Meet Trainers</a>
                            </li>
                            <li class = "nav-item">
                              <a class= "nav-link" href="{{url_for('social.feed')}}">| Feed</a>
                            </li>
                            <li class="nav-item">
                              <a class="nav-link" href="{{url_for('main.battle')}}">| Gym</a>
                            </li>         
                          {% endif %}
                        {% endif %}

                        </ul>
                        {% if current_user.is_authenticated %}
                        <ul class="navbar-nav ms-auto">
                          <a href="{{url_for('social.my_posts')}}">
                              <img src="{{current_user.get_icon_url()}}">
                                <li class = "nav-item">
                                  <div class = "row">
                                  <div class="col-12">
                                  <a class = "nav-link" href="{{url_for('auth.logout')}}">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="35" height="35" fill="currentColor" class="bi bi-box-arrow-right" viewBox="0 0 16 16">
                                      <path fill-rule="evenodd" d="M10 12.5a.5.5 0 0 1-.5.5h-8a.5.5 0 0 1-.5-.5v-9a.5.5 0 0 1 .5-.5h8a.5.5 0 0 1 .5.5v2a.5.5 0 0 0 1 0v-2A1.5 1.5 0 0 0 9.5 2h-8A1.5 1.5 0 0 0 0 3.5v9A1.5 1.5 0 0 0 1.5 14h8a1.5 1.5 0 0 0 1.5-1.5v-2a.5.5 0 0 0-1 0v2z"/>
                                      <path fill-rule="evenodd" d="M15.854 8.354a.5.5 0 0 0 0-.708l-3-3a.5.5 0 0 0-.708.708L14.293 7.5H5.5a.5.5 0 0 0 0 1h8.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3z"/>
                                    </svg>
                                    <h6>Logout</h6>
                                  </a>
                                  </div>
                                  <div class = "col-12">
                          </a>
                                </li>
                        </ul>
                                    {% endif %}                          
                                  </div>
                </div>
            </nav>